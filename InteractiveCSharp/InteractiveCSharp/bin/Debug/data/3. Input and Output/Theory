Вывод текста в консоль:

      //1 вывод текста
        Console.Write("Наш текст");
        Console.ReadKey();

 

Вывод текста и значений в консоль:

        //2 вывод значений
        string name = "Professor VB"; //текстоваЯ переменная
        Console.Write("name = " + name);
        Console.ReadKey();

 

Ввод и вывод значений в консоль:

        //3 ввод и вывод значений
        Console.Write("Скажи свое имя  " );
        string name2 =Console.ReadLine(); //текстоваЯ переменная

        Console.Write("Скажи свой Никнейм  ");
        string name3 = Console.ReadLine(); //текстоваЯ переменная

        // 1 вар - запись как VB.net
        //Console.Write("Имя - " + name2 + ", а ник - " + name3);

        // 2 вар - запись в стиле С - намного удобней
        Console.Write("Мое имя {0}, а ник - {1}", name2, name3);

        Console.ReadKey();



Ввод и вывод числовых значений:     

  //4 ввод и вывод числовых значений
        Console.Write("Число 1 = ");
        int x = int.Parse(Console.ReadLine()); //числовая переменная

        Console.Write("Число 2 = ");
        int y = int.Parse(Console.ReadLine()); //числовая переменная

        int z = x + y;

        Console.Write("{0} + {1} = {2} ", x, y,z);

        Console.ReadKey();

Контрольные вопросы:
1) Что будет выведено в следующем примере?
int x=3,y=2,z=4;
Console.Write("{0} + {1} = {2} ", x, y,z);

1 - 3 + 2 = 4
2 - 3 + 2 = 5
3 - 2 + 3 = 4